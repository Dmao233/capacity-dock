# 菜单栏消耗概览验收

日期：2026-09-07。范围：菜单栏 NSPopover，420 × 660 pt；侧栏额度小工具和独立设置页保持原布局。

## 参考与实际证据

- 用户选定的浅色与暗色方向：`/Users/lu/.codex/generated_images/01a07a1e-1c3e-7183-802a-890bf5978fa1/exec-dea35142-2699-4b24-8dcd-6b79f51a9f01.png`、`exec-393a50f1-910a-45d1-9b39-ab68413be254.png`。
- CodeBurn 实际参考：`/Users/lu/Documents/GitHub/codeburn/src/codex-cache.ts`、`windows/src/components/TrendInsight.tsx`，以及用户提供的趋势与悬停截图。复用设计思路和缓存策略，不导入 CodeBurn 缓存或更换计价规则。
- [Rare UI GitHub Activity](https://www.rareui.com/components/githubactivity)：浏览器实际打开演示并读取页面展示的源码。默认 cellSize=11，gap=round(cellSize/4)=3，圆角 3，颜色强度 0.30/0.52/0.76/1。
- 全部本地实机证据位于 `.build/popover-qa/`：`rare-activity-reference.png`（1280×720）、`final-rare-light.png`（446×686）、`final-dark-today-trend.png`、`final-light-activity-tooltip.png`、`final-week.png`、`final-month.png`。
- 已实际打开并检查合成对比图：`compare-activity-before.png`、`compare-final.png`、`compare-dark-final.png`。包含整体和方格/悬停明细局部对比，不仅凭源码判断。
- 原设计含菜单栏背景与模拟数据；裁切内容区、缩放到约 420 pt 比较。原组件方格局部按 1:1 像素比较。实机图包含系统弹窗外沿，内容为 420×660 pt。真实金额随日志增长，截图金额不要求相同。

## 迭代与结果

| 问题 | 等级 | 修正与验收 |
|---|---|---|
| 周/月扫描缓慢、历史读取内存暴涨 | P0 | 每 64 KB 流式读取块独立释放 Foundation 临时对象；共享串行读取器、文件指纹缓存、各周期完成缓存和任务合并。实际冷扫描完成，暖缓存周/月约 1 秒；实机周期切换有结果。 |
| Swift Charts 数值域配置触发退出 | P0 | 构成图改为字符串分类域；实机多次打开、切换无该断言。 |
| 活动只有一天、趋势随今天变成单柱 | P2 | 活动与趋势共用独立近 30 天窗口，汇总和下方列表仍随周期切换；今日实机趋势有连续历史柱。 |
| 活动格过大、长方形、两行 | P2 | 最终按 Rare UI 固定 11×11 pt、3 pt 间距，三行十列，禁止柔性拉伸；最终浅色实机截图及局部对比已确认。 |
| 缺少悬停二级信息 | P2 | 按天按模型汇总，趋势/活动共享日期、总用量、模型用量和金额浮层；未知价格显示破折号。实际浅色活动及暗色趋势浮层已确认。 |
| 顶部服务商筛选重复、页脚英文、信息稀疏 | P2 | 去掉顶部筛选；保留下方模型/服务商分组，紧凑四指标、中文页脚和固定币种入口。 |

## 五项视觉检查

- 字体：使用原生 SF 和等宽数字；大金额、标题、次级用量有清楚层级。按用户要求比初稿更紧凑；真实模型名沿用日志规范。
- 布局：顶部汇总固定，下方列表独立滚动，币种页脚固定；30 天小方块居中，不为铺满宽度而放大。近七天、本月实机汇总正常显示。
- 颜色：保留用户选定的浅色青绿/暗色紫色方向；活动采用 Rare UI 离散四档深浅，无记录有浅底虚线边界，区别实测零。
- 资产：使用项目已有服务商图标与系统符号。流动球仅刷新时播放，空闲/减少动态效果时停止，不增加常驻连续绘制。
- 内容：真实本机日志；不填充虚构历史数据。独立图表标明近 30 天，日期范围明确。原组件的 GitHub 仓库页脚没有照搬到 AI 账单，下方已有模型与服务商明细；这是保留既定产品范围的适配。

## 验证与边界

- `swift test -c release`：187 项测试、18 个套件通过，日志 `/tmp/capacity-rare-final-tests.log`。覆盖日期/DST、30 天范围、缺失数据、模型身份、每日模型汇总、未知价格、缓存有效期、大缓存和流式边界。
- 真实日志基准：旧扫描采样物理占用 15.9 GB；加入每块释放后冷扫描采样 58 MB、当时峰值 110.7 MB。冷周扫描约 120 秒，暖缓存今天/周/月分别 1.08/1.16/0.93 秒。它们是特定扫描测量，不能当作完整应用的内存值。
- 完整应用 v7 多次切换后物理占用 496.1 MB、该进程峰值 1.1 GB，证据 `final-memory-v7.txt`。最终 v8 仅调整活动格尺寸与颜色等级，未重复做长期内存测试；不声称已经完成长时间泄漏观测。
- 测试包位于 `/Users/lu/Library/Caches/CapacityDock/LocalTests/popover-v8/CapacityDock.app`，签名严格验证通过。未推送。
- 最终小方块采用用户指定三行而非原组件七行；保留近 30 天而非全年。两者均是用户明确要求。

未发现阻断交付的 P0/P1/P2 视觉问题。长时间常驻内存观察仍属于后续耐久验证，当前只报告已测证据。

final result: passed
